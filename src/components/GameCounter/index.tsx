import { ReactElement, useEffect } from 'react';

import './styles.css';

export const GameCounter = (): ReactElement => {
  useEffect(() => {
    // setinterval
  });

  return <span className='Game-counter'>00:00</span>;
};
